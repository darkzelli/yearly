import 'package:flutter/material.dart';

class Recap extends StatelessWidget {
  const Recap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('recap'));
  }
}
